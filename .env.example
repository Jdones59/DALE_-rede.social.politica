# Example .env (DO NOT COMMIT secrets) — copy to .env and fill with real values
# PostgreSQL
POSTGRES_USER=joao
POSTGRES_PASSWORD=change_this_to_a_secure_password
POSTGRES_DB=dale_db

# Prisma / backend DB URL (use this when running the backend outside Docker)
# Format: postgresql://<user>:<password>@<host>:<port>/<db_name>?schema=public
DATABASE_URL=postgresql://joao:change_this_to_a_secure_password@postgres:5432/dale_db?schema=public
